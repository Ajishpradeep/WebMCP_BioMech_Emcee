# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.venv-pipeline

# ── NEVER COMMIT MODEL WEIGHTS ──────────────────────────────────
# SAM 3D Body checkpoints are under Meta's SAM License and must not
# be redistributed. See .claude/steering/tech.md §7.
pipeline/checkpoints/

# Vendored upstream repo (cloned by pipeline/setup.sh, not ours to ship)
pipeline/vendor/

# ── Large / generated pipeline artifacts ────────────────────────
pipeline/data/frames/
pipeline/data/raw/
pipeline/out/

# ── Source footage: local inputs, not committed ─────────────────
# See ATTRIBUTION.md. Keep even expressly licensed candidate binaries local until one passes QA;
# retain the small provenance/readme record so source and license details are not lost.
input_baseball/*
!input_baseball/licensed_candidates/
input_baseball/licensed_candidates/*
!input_baseball/licensed_candidates/README.md

# OS noise
.DS_Store

# Node
node_modules/
web/dist/

# Uploaded clips (scratch, local backend only)
pipeline/data/uploads/
web/tsconfig.tsbuildinfo
# Local Devpost workflow progress (not project source or submission content)
.devpost-hackathon-state.json
